/**
 * Root route `/` — this file must exist to avoid Next.js build warnings,
 * but the actual content is served by `(dashboard)/page.tsx` via the
 * route-group layout. This component is never rendered.
 */
export { default } from '@/app/(dashboard)/page';
