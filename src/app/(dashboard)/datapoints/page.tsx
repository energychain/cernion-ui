import { ComingSoon } from '@/components/shared/empty-state';

export default function DatapointsPage() {
  return (
    <ComingSoon
      title="Datenpunkte"
      description="CRUD, Health-Übersicht, Snapshots und OE-Metadata — verfügbar in v0.20.4"
    />
  );
}
