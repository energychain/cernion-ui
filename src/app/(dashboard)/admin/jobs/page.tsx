import { ComingSoon } from '@/components/shared/empty-state';

export default function AdminJobsPage() {
  return (
    <ComingSoon
      title="Job-Tracker"
      description="Laufende und abgeschlossene Async-Jobs — verfügbar in v0.20.6"
    />
  );
}
