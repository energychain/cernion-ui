import { ComingSoon } from '@/components/shared/empty-state';

export default function AdminTokensPage() {
  return (
    <ComingSoon
      title="API-Token-Verwaltung"
      description="Token erstellen, anzeigen und widerrufen — verfügbar in v0.20.6"
    />
  );
}
