import { ComingSoon } from '@/components/shared/empty-state';

export default function MarketPage() {
  return (
    <ComingSoon
      title="Energiemarkt"
      description="Spotpreise, CO₂-Intensität und erneuerbare Erzeugungsprognosen — verfügbar in v0.20.2"
    />
  );
}
